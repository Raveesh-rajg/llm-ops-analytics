"""TokenLedger — LLM operations analytics: cost, quality, latency.

Analytics ON AI systems, not just analytics WITH them. Traces follow
OpenTelemetry GenAI semantic-convention field names (gen_ai.*) so the schema
matches what real instrumentation emits; the source is this portfolio's own
analytics agent instrumented end-to-end, scaled with a seeded generator.
"""

__version__ = "0.1.0"
